name: Release

on:
  push:
    tags:
      - "v*"

jobs:
  release:
    name: Release
    uses: sxzz/workflows/.github/workflows/release.yml@v2
    with:
      publish: true
      build: pnpm run build
    permissions:
      contents: write
      id-token: write
